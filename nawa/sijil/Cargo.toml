[package]
name = "nawa-sijil"
version = "0.0.1"
edition.workspace = true
license.workspace = true
description = "SIJIL (سجل) — the kernel's flight recorder: gate-written, append-only, agents cannot self-report"

[dependencies]
nawa-core = { path = "../core" }
