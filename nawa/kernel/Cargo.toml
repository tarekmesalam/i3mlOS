[package]
name = "nawa-kernel"
version = "0.0.1"
edition.workspace = true
license.workspace = true
description = "The i3ml kernel binary — safe Rust over the NAWA trusted core"

[[bin]]
name = "nawa"
path = "src/main.rs"

[dependencies]
nawa-core = { path = "../core" }
