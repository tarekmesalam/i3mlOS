[package]
name = "nawa-aman"
version = "0.0.1"
edition.workspace = true
license.workspace = true
description = "AMAN (أمان) — the capability broker: unforgeable authority, attenuate-only delegation, consent for the irreversible"

[dependencies]
nawa-core = { path = "../core" }
nawa-sijil = { path = "../sijil" }
