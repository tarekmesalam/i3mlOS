[package]
name = "xtask"
version = "0.0.1"
edition.workspace = true
license.workspace = true
description = "Dev-host runner: build the kernel, assemble the ESP, boot QEMU, assert the serial line, enforce the framekernel rule. Zero dependencies."
