[package]
name = "i3ml-sha256"
version = "0.0.1"
edition.workspace = true
license.workspace = true
description = "SHA-256 from scratch — the hash that chains SIJIL, validated against the published vectors"
